machine:
  ruby:
    version: 2.1.3

dependencies:
  pre:
    - git clone git@github.com:transitland/onestop-id-registry-validator.git
    - cd onestop-id-registry-validator && bundle install

test:
  override:
    - cd onestop-id-registry-validator && bundle exec rake
