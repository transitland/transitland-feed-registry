machine:
  ruby:
    version: 2.1.2

database:
  override:
    - echo "we need no database" 

test:
  override:
    - bundle exec rake

deployment:
  production:
    branch: master
    commands:
      - bundle exec rake report:build
      - bundle exec s3_website push --config-dir=./config
